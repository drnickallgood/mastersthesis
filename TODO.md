- Look into Bucket numbers for encoding the topk hashes
- encode this into a matrix / state vector for the quantum machines to use
- Implement Grover's search with Qiskit module to search for the (CRC or ngram?)


